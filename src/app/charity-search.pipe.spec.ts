import { FilterPipe } from './charity-search.pipe';

describe('CharitySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipe();
    expect(pipe).toBeTruthy();
  });
});
